<% provide(:title, 'All Students') %>
<h1>All Students</h1>

<%= will_paginate :class => 'pagination pagination-centered pagination-small' %>

<ul class="students">
  <%= render partial: 'student', collection: @students, as: :student %>
</ul>

<%= will_paginate :class => 'pagination pagination-centered pagination-small' %>