<% provide(:title, @student.name) %>
<div class="row">
  <aside class="span2">
    <section>
      <h1>
        <%= @student.name %> | <%= @student.grade %>
      </h1>
    </section>
  </aside>
  <div class="span10">
    <% if @student.votes.any? %>
      <%= render 'vote_record' %>
    <% else %>
      <h3>Go vote!</h3>
    <% end %>
  </div>
</div>